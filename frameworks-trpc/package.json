{
  "name": "speakeasy-example-trpc",
  "version": "2.0.0",
  "description": "",
  "main": "server/index.js",
  "scripts": {
    "start": "tsx server/index.ts",
    "generate-openapi": "tsx server/generateOpenApi.ts > openapi-spec.json",
    "preview-openapi": "npm run generate-openapi && npx @scalar/cli document serve openapi-spec.json",
    "generate-openapi-and-sdk": "npm run generate-openapi && speakeasy generate sdk --schema openapi-spec.json --lang typescript --out ./sdk",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Phil Sturgeon <phil@apisyouwonthate.com>",
  "license": "ISC",
  "dependencies": {
    "@trpc/client": "^11.18.0",
    "@trpc/server": "^11.18.0",
    "express": "^5.2.1",
    "trpc-to-openapi": "^3.3.0",
    "zod": "^4.0.0"
  },
  "devDependencies": {
    "@types/express": "^5.0.6",
    "tsx": "^4.23.1",
    "typescript": "^5.2.2"
  }
}
